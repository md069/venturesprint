{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "turbo build",
  "ignoreCommand": "exit 0",
  "rewrites": [
    { "source": "/app/:path*", "destination": "apps/app/:path*" },
    { "source": "/:path*", "destination": "apps/web/:path*" }
  ]
}
